---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name: "Cardano Stake Pool Ansible role"

# Short description of this project
description: |-
  Ansible role to perform all the operations of creating, operating and running a Cardano stake pool. The aim of this playbook is to automate the repeatable tasks in a production environment to reduce human error and operational complexity.

# Canonical GitHub repo
github_repo: osodevops/ansible-role-cardano-stake-pool

related:
  - name: "Cardano node installer"
    description: "Ansible role to install Cardano binaries from source"
    url: "https://github.com/osodevops/ansible-role-cardano-node"
