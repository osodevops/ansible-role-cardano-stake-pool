---
#
# This is the canonical configuration for the `README.md`
# Run `make readme` to rebuild the `README.md`
#

# Name of this project
name: "Cardano stake pool role"

# Short description of this project
description: |-
  This is a docker wrapper for the Confluent supported ansible playbooks! These supported ansible Playbooks for Confluent Platform offers a simple way to configure and deploy Confluent Platform.

# Canonical GitHub repo
github_repo: osodevops/ansible-role-cardano-stake-pool

related:
  - name: "Cardano stake pool"
    description: "cardano stake pool role "
    url: "osodevops/ansible-role-cardano-stake-pool"
